import { StubPage } from "@/components/clavis/stub-page";

export default function HistoriquePage() {
  return (
    <StubPage
      title="Historique"
      step="1.13"
      description="Journal complet de tous les mouvements (remises, retours, pertes, refaites)."
    />
  );
}
